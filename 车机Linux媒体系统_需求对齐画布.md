# 需求对齐画布：车机 ↔ Linux 媒体互联系统

> 状态：需求对齐中（v0.2 草稿） ｜ 用途：自己用（非商业）

## 1. 产品概念（Why）
- **一句话**：把独立 Linux 设备作为「媒体大脑」，车机 APK 作为它的「显示屏 + 遥控器」。
- **解决的问题**：车机性能/生态有限，把媒体处理与业务逻辑下沉到 Linux，车机只负责采集状态与呈现界面，降低车机负载、便于统一升级与扩展。
- **用户**：个人项目，非商业分发。

## 2. 已对齐的关键假设（来自你的回答）
| 维度 | 结论 |
|------|------|
| 形态 | 独立 Linux 设备（物理独立于车机，类似树莓派/工控机） |
| 通道 | 车机有 USB + WiFi 可用（APK↔Linux 通道）；车辆信号经 OBD + 车身 CAN 接入（Linux 解析） |
| 核心场景 | 媒体/内容投屏控制：车机发媒体状态 → Linux 处理 → 回控/展示播放界面 |
| 实时性 | 混合（控制指令近实时，媒体元数据可放宽）→ 需分层 |
| UI 形态 | 数据可视化大屏（图表/仪表/列表为主，偏“看”，**横屏**） |
| 投屏方向 | APK 作**客户端**，Linux 作**服务端**，APK 从服务端**拉取**数据渲染 |
| 车型 | **长安欧尚 Z6 · 燃油版**（OnStyle 智慧座舱，底层 Android，横屏中控，8155 级芯片） |

## 2.1 APK 界面布局（原型 v0.3 已实现）
横屏单屏，自上而下三段式 + 中部上下滑动双页：
- **顶部状态栏**：服务端连接状态 + 车速/电量 + **服务端负载 / 温度 / 延时** + 时钟（负载/温度/延时来自 Linux 侧，APK 拉取渲染）。
- **中部主区域（可上下滑动切换两页）**：
  - 页 0 · 音乐：左 **1/3 = 车模型 + 车辆状态**（顶视车模，开门/开窗在模型上实时显示）；右 **2/3 = 音乐**（正在播放 / 频谱+控制 / 播放列表 本地·DLNA·在线）。*原型车模已按长安欧尚 Z6 真实比例绘制（车长4699×车宽1890×车高1680mm，比例≈2.49:1；轴距2795mm 占车长59.5%，轮距1603mm 占车宽84.8%，5门SUV/前置前驱/20寸轮245/50 R20），保留开门(外移变橙)/开窗(玻璃变暗)实时交互。3D 模型暂缓（用户 13:42 确认）。*
  - **车辆状态区字段（13:48 用户指定，按 Linux 服务端下发渲染，原型已落地）**：
    - **动力**：车速(km/h)、转速(r/min)、挡位、油表(%)、电瓶(12V)、里程
    - **胎压 (bar)**：四轮 FL/FR/RL/RR，含低压告警配色（<2.0 bar 标红）
    - **车身**：门(开/4)、窗(开/4)、大灯(近光/关)、双闪(开/关)、空调(°)、门锁(已锁/未锁)
    - 顶部状态栏同步一瞥显示：车速 / 转速 / 油量% / 电瓶V
  - 页 1 · 导航（**调起外部 App**）：**导航调用车机里已安装的导航 App（外部 App），APK 不自绘底图**。在音乐页**上滑 → 调起/切换车机导航 App 全屏运行**（提供导航 App 选择器）；**点最左侧悬浮返回按钮 / 收起 dock → 导航 App 转后台、APK 回前台显示音乐/车状态**；导航 App 后台持续运行。回 APK 后音乐页顶部显示**迷你导航卡片（转向/ETA/剩余里程）**（APK 后台高德 SDK 静默取数，见 §8）。
  - 音乐/车状态页的数据仍来自 Linux 服务端（界面即「Linux 的屏幕」）；导航渲染与导航引擎在外部 App，Linux 仍可作「大脑」算路/下发目的地。
- **底部居中 Dock 栏**：主页 / 音乐 / 导航 / 设置 圆形按钮居中排列，点音乐/导航可直接跳对应页。

## 2.2 车型与硬件约束（长安欧尚 Z6 燃油版）
| 项 | 结论 / 影响 |
|----|------------|
| 车机底层 | Android（OnStyle 定制），第三方 APK 可正常安装运行 |
| 芯片 | 骁龙 8155 级旗舰车机芯片 → **WebView 套 H5 性能完全够用**，进一步支撑 WebView 方案 |
| 屏幕 | 横屏中控屏 → 与当前横屏三段式 UI 一致 |
| GPS | 车机原生 GPS 模块 → 高德定位可用（已确认上报 Linux） |
| 动力 | **燃油版（无动力电池）** → 原“电量”字段已定：改为 **12V 小电瓶电压 + 油箱油量（按加油记录推算）**（见 §8 阻断项②） |
| 车辆数据 | 长安欧尚**未开放第三方 APK 车辆信号（门/窗/车速/挡位）API** → 普通 APK 拿不到，需另定获取方式（见 §8 阻断项①） |
| 温控/发热 | 车规 8155 耐温设计（-40~105°C）；但 **WebView 持续动画 + 暴晒高温(车内 60-70°C) 可能触发降频掉帧/卡顿**。mitigation：动画节流、高温自动降级（动态壁纸为首选关闭项）、优先原生渲染（见 §7 技术路线修正） |
| 俯视车模参考尺寸 | 原型左 1/3 车模按上述真实尺寸比例绘制（车身矩形+四轮+悬浮车顶整片玻璃+前格栅+尾翼），作为状态页视觉锚点；门/窗/灯状态由 Linux 经 OBD/CAN 下发后实时覆盖（开=外移变橙，窗开=玻璃变暗）。 |

## 3. 范围
### 3.1 In-Scope（优先级分层）
- **P0 核心（必须做）**
  - 音乐大屏：正在播放 / 频谱 / 控制 / 播放列表（本地·DLNA·在线）
  - 高德导航：调起车机已装导航 App 全屏运行（上滑开/下滑后台），APK 负责调起与导航状态感知（GPS=车机原生，由导航 App 使用）
  - 车状态显示：车模型 + 门/窗/车速/挡位/小电瓶电压/油量推算（见 §8 阻断项①数据来源）
- **P1 加分项（锦上添花）**
  - 动态壁纸：持续动画，属高温降级策略中**优先关闭**项（见 §2.2 温控约束）
- **通信/服务端（仅当选 B/C/D 保留 Linux 时）**
  - 车机 ↔ Linux 双向通道（USB/WiFi）、状态同步、断线重连

### 3.2 Non-Goals（本次不做，防范围蔓延）
- 不替代车机原厂娱乐系统
- 不做媒体内容本身的版权获取/下载
- 不做多车机/多用户并发
- 不依赖逆向代码直接复用（独立实现，clean-room 思路）

## 4. 功能模块拆分
- **通信层**：传输协议 + 序列化 + 心跳/重连
- **车机端（APK，瘦客户端）**
  - 数据采集：媒体播放状态（曲目/进度/音源/音量等）
  - 展示层：可视化大屏（当前播放、播放列表、进度、媒体库浏览）
  - 交互层：基础控制（播放/暂停/切歌/音量）
- **Linux 端（服务端）**
  - 媒体处理/聚合（对接媒体源、解析、转码/选择）
  - 状态管理 + 下行指令/界面数据生成（APK 拉取）
  - 可选：媒体服务（DLNA / 文件服务 / 播放引擎）

## 5. 数据流（APK 客户端 ↔ Linux 服务端）
```
[APK 客户端]  ── 拉取(PULL) ──▶  界面数据 / 播放状态   ◀── [Linux 服务端 处理/聚合]
[APK 客户端]  ── 控制指令(POST) ─▶  播放/暂停/切歌/音量 ──▶ [Linux 服务端 执行/转发]
```
- 大屏展示的「频谱 / 服务端负载 / 温度 / 码率 / 延迟」等，均为 Linux 侧产生、APK 拉取后渲染。

## 6. APK 与 Linux 职责边界
- **APK（瘦客户端）**：采集 + 渲染 + 轻交互；不含核心媒体处理逻辑
- **Linux（大脑/服务端）**：媒体处理/业务逻辑/状态源；下发“界面该显示什么”

## 7. 建议技术路线（**架构已确认：保留 Linux 服务端 + APK 瘦客户端**）
- **通道**：优先 WiFi 局域网（同网段）；USB 作为备选高稳通道。协议建议轻量（WebSocket / REST 或 MQTT），混合实时性用同一通道分层（控制 QoS 高、元数据低）。
- **APK**：原型阶段用 H5（HTML/CSS/JS）快速验证；正式技术栈待选 原生 Kotlin(Compose) / Flutter / WebView 套 H5。
  - ⚠️ **发热权衡（修正此前 WebView 唯一最优判断）**：Z6 暴晒高温场景下，WebView 持续动画（频谱/遥测跳动/高德 JS 底图）比原生渲染更费 CPU/GPU，易触发降频。若用户重视车机温控稳定，**原生 Kotlin(Compose) 或 Flutter 更优**；WebView 方案须加 动画节流 + 高温降级策略（监听车机温度，超阈值关非必要动画）。
- **Linux（服务端，已确认保留）**：常驻服务（Go / Python / Node 任选）暴露 WS/HTTP 接口，对接媒体源 + 车辆信号。
- **序列化**：JSON（调试友好），高频帧可换二进制。

## 7.2 车辆信号接入（OBD + CAN，Linux 服务端负责解析）
**架构价值点**：APK 不适合直接啃硬件/底层协议，OBD + CAN 的报文解析统一收口在 Linux 服务端，APK 只消费解析后的结构化状态 → 解决阻断项①，且全量车辆信号可读。
- **OBD（动力信号）**：ELM327（蓝牙/USB）接车机 OBD-II 口，Linux 解析 PID → 车速 / 转速 / 水温 / 瞬时油耗 / 故障码(DTC) / 12V 电瓶电压。
- **CAN（车身信号）**：接**车身 CAN 总线**（舒适 CAN，非标准 OBD 暴露的那条）→ 门状态 / 窗状态 / 大灯 / 双闪 / 空调 / 门锁。
  - ⚠️ **工程提醒**：车身 CAN 通常不在标准 OBD-II 引脚暴露，需额外硬件接舒适 CAN（如 ESP32/MCP2515 或 USB-CAN 适配器接对应 CAN 线），并逆向/采集该车型 CAN 报文 ID 与字节定义（长安欧尚专属，需实车嗅探标定）。这一步是项目最硬的工程难点。
- **数据流**：OBD/CAN 硬件 → Linux 解析 → WS 推 APK → 渲染车模型+状态卡。
- 与阻断项②联动：12V 电瓶电压由 OBD 真读；油箱油量仍按加油记录推算（APK 输表单 / Linux 算）。

## 7.1 导航接入方案（**架构变更：调起车机已装导航 App，APK 不自绘底图**）
**变更要点（用户决策）**：导航不再由我们的 APK 内嵌高德 JS API 自绘底图，而是**调起车机里已安装的导航 App（外部 App）**作为全屏导航界面；APK 退为「调起方 + 导航状态感知方」。
- **交互模型（用户定义）**：在音乐页**上滑 → 调起/切换到车机导航 App，全屏前台运行**（导航开始/继续）；在导航 App 中**下滑 → 导航 App 转入后台，APK 回到前台显示音乐/车状态**，导航 App 仍在后台持续运行（定位/语音由该 App 自身保证）。
- **分工原则（保留 Linux=大脑）**：Linux 服务端仍可调高德 Web服务 API 算路并产出目的地/route，通过 Intent URI（如 `amapuri://`）传给外部导航 App 发起导航；或用户直接在导航 App 内搜目的地。外部 App 负责地图渲染 + 导航引擎 + 实时转向/ETA 播报。APK 负责 Intent 调起、感知导航 App 前台/后台状态、在音乐页展示「导航运行中」或迷你信息。
- **关键约束（实现前必须确认，见 §8 导航难点）**：外部导航 App 具体包名/调起方式需确认；因 APK 不自绘底图，**高德 JS API key 可不再需要**，若 Linux 仍做算路则需高德 Web服务 API key（用户暂缓）；车机需联网供导航 App 拉取地图；原型阶段已接高德 JS API 仅作占位兜底，后续改为 Intent 调起逻辑。

## 7.3 实时性分层方案（混合分层：控制高 QoS、元数据低）
| 数据类型 | 实时性要求 | 建议刷新率 | 通道 | 触发方式 |
|---------|-----------|-----------|------|---------|
| 控制指令（播放/暂停/切歌/音量/页切换） | 近实时 <200ms | 即时 | WS 上行(APK→Linux) | 事件触发 |
| 车辆动力（车速/转速/水温/油耗） | 准实时 | 5–10Hz（100–200ms） | WS 下行推 | 周期+变化 |
| 车辆车身（门/窗/灯/双闪/空调/锁） | 事件级 | 变化即推 | WS 下行推 | 事件触发（变化才发，零持续带宽） |
| 故障码 DTC | 事件级 | 出现/清除即推 | WS 下行推 | 事件触发 |
| 音乐元数据（曲目/进度/音源） | 秒级 | 1–2s | WS 下行推 | 变化才更 |
| 频谱 | 显示级 | 10–15fps（兼顾发热） | WS 下行推/本地算 | 周期 |
| 导航（转向/ETA/剩余里程） | 准实时 | 转向变化即推 / ETA 1–2s | WS 下行推 | 事件+周期 |
| 高德底图 GPS 跟随 | 准实时 | 1–5Hz | APK 本地 GPS | 周期 |
| 遥测（Linux 负载/温度/延时） | 秒级 | 1–2s | WS 下行推 | 周期 |

**关键结论**：
- **网络非瓶颈**：同网段 WiFi 单向延迟 1–5ms，USB 更低；整链路（采集→Linux→WS→渲染）控制指令端到端 <50ms，人感知无延迟。
- **真正下限在采集端**：ELM327(OBD) 稳定轮询上限 ~10Hz → 车速/转速显示匹配 5–10Hz 即可；CAN 直连更高但显示不需要。
- **车身状态事件驱动**：门/窗/灯变化才推，最省带宽与算力。
- **发热联动**：频谱/动画节流 10–15fps，既够看又控温（呼应 §7 发热权衡）。
- **协议建议**：控制与状态均走 WebSocket（服务端主动推），不要用 APK 轮询 HTTP；高频帧（车速/频谱）序列化可换二进制降开销。

## 8. 已确认 / 待确认
- ✅ **投屏方向（已确认）**：APK 作客户端，Linux 作服务端，APK 从服务端拉取数据渲染。架构即「Linux = 数据/逻辑源，APK = 瘦客户端 UI」。
- ✅ **导航接入（架构已定）**：**调起车机已安装导航 App（外部 App）**，APK 不自绘底图。上滑开导航全屏、点悬浮按钮转后台（导航后台持续运行）。Linux 仍可作大脑算路/下发目的地，APK 负责调起与状态感知。原型当前高德 JS API 仅占位，需改为 Intent 调起逻辑。
- ✅ **导航交互方案（已定）**：
  - **① 切回方式 = 悬浮层（解决手势不可控）**：导航 App 全屏期间，我们的 APK 以**悬浮窗（SYSTEM_ALERT_WINDOW）常驻**：屏幕**最左侧一个悬浮返回按钮**（点击 → 回到 APK 音乐页，导航 App 转后台）；**dock 栏以悬浮形态浮在导航 App 上方、可隐藏/收起**（主页/音乐/导航/设置，点即切回对应页）。不依赖外部 App 的手势。
  - **② 回 APK 显示迷你导航卡片（已选）**：回音乐页后显示**迷你导航卡片（转向/ETA/剩余里程）**。为此 APK 须**自建后台导航数据服务**（高德 Android SDK 前台服务取数）；⚠️ 双导航/双语音风险：我们的后台 SDK 以**静默模式（禁用 TTS 播报、仅监听 turn-by-turn 数据）**运行，语音播报只由外部导航 App 负责，避免双重播报。
  - **③ 目的地来源**：以**用户在外部 App 内直接搜**为主（APK 不强制算路）；Linux 算路后通过 Intent 带目的地参数调起为**可选增强**（如 `amapuri://` 带终点）。
  - **④ 外部 App 身份 = 提供导航 App 选择器**：不绑定单一包名，APK 提供**导航 App 选择器**（高德车机版/百度/系统默认 chooser），用户自选调用哪个；Intent 按所选 App 的 URI scheme 调起。
  - **权限依赖**：悬浮窗需 `SYSTEM_ALERT_WINDOW` 授权（车机 Android 通常可授权）；需记入 APK 权限清单。
- ⏳ **媒体源与协议（用户后补）**：具体媒体源（本地/DLNA/在线）与通信协议细节待补充，技术架构方案阶段再定。
- ❓ **APK 技术栈（待选）**：原生 Kotlin(Compose) / Flutter / WebView 套 H5 三选一；原型阶段先用 H5 实现以快速验证。欧尚 Z6 为 8155 芯片，WebView 性能无忧，**WebView 套 H5 仍是最优解**。
- ✅ **阻断项① 车辆状态数据来源（已定：Linux 接 OBD + CAN）**：用户确认**保留 Linux 服务端**，并**考虑 OBD + CAN** 接入车辆信号。即回到最初架构「车机↔Linux 双向通讯，APK 是 Linux 界面」——Linux 啃硬件/底层协议，APK 只消费解析后状态。
  - OBD（ELM327）→ 车速/转速/水温/油耗/故障码/12V 电瓶电压
  - CAN（车身舒适 CAN）→ 门/窗/大灯/双闪/空调/门锁（⚠️ 需额外硬件接舒适 CAN + 车型报文标定，见 §7.2 工程提醒）
  - 已排除 C（模拟）；A（系统权限）不再需要，因解析收口在 Linux。
- ✅ **阻断项② 燃油版“电量”字段（已定）**：改为 **12V 小电瓶电压 + 油箱油量**。
  - **油箱油量（推算，不依赖实时油位传感器）**：用户每次加油时输入「本次加油量(L) + 当前里程(km)」，由 Linux 服务端记录并计算：
    - 平均油耗 = 两次加油里程差 / 本次加油量
    - 剩余油量 = 上次加油量 − 期间估算消耗（期间里程 × 平均油耗）
    - 续航(km) = 剩余油量 / 平均油耗 × 100
  - 输入入口在 APK（加油记录表单），计算与存储放 Linux 服务端（贴合“Linux=大脑”）。原型当前用模拟值，待真实表单接入。
  - ⚠️ **12V 小电瓶电压**：真实读取同样受阻断项①约束（第三方 APK 读不到，需 OBD 或系统权限）。若阻断项①选 C(模拟)，则电瓶电压也走模拟值；若选 A/B，可由对应通道真读。

## 9. 下一步交付物（已选 C）
- ✅ **C. APK 界面原型**（横屏三段式：顶部状态栏含负载/温度/延时 + 中部音乐/导航上下滑动双页 + 底部居中 Dock）。产出：`apk-dashboard-prototype.html`（已更新：导航页改为「调起外部 App 启动面板」+ 外部 App 全屏 overlay + 悬浮层「最左返回按钮 + 可隐藏 dock」+ 音乐页顶部「迷你导航卡片」）
- 待媒体源/协议补齐、技术栈确定后，再推进 A（技术架构方案）与 B（PRD）。
